# In-tree TDX guest driver provides /dev/tdx_guest and the ConfigFS TSM
# report interface (/sys/kernel/config/tsm/report), replacing the former
# out-of-tree mod-tdx-guest module.
CONFIG_TDX_GUEST_DRIVER=y
CONFIG_TSM_REPORTS=y
