# Minimal CoCo Attestation Agent config for dstack guest images.
# This first integration only enables local evidence generation; token/KBS
# semantics are intentionally left independent from dstack KMS.

[eventlog_config]
init_pcr = 17
enable_eventlog = false

[log]
level = "info"
